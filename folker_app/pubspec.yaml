name: folker_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.1.1

dependencies:
  camera: ^0.9.4+19
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  image_input: ^0.0.5
  permission_handler: ^11.3.1
  file_picker: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/learing_any_programming_language.jpg